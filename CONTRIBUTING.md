# Contributing

Thanks for contributing to this Gradle multi-project Spring Boot repo (Java 25).

## Requirements

- JDK **25** only (see `.sdkmanrc`)
- Use `./gradlew` / `gradlew.bat`

## Build

```bash
./gradlew clean test
```