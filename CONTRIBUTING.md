# Contributing

Thanks for contributing to this Gradle multi-project Spring Boot repo (Java 11).

## Requirements

- JDK **11** only (see `.sdkmanrc`)
- Use `./gradlew` / `gradlew.bat`

## Build

```bash
./gradlew clean test
```