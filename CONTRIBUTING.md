# Contributing

Thanks for contributing to this Gradle multi-project Spring Boot repo (Java 17).

## Requirements

- JDK **17** only (see `.sdkmanrc`)
- Use `./gradlew` / `gradlew.bat`

## Build

```bash
./gradlew clean test
```