# Contributing

Thanks for contributing to this Gradle multi-project Spring Boot repo (Java 21).

## Requirements

- JDK **21** only (see `.sdkmanrc`)
- Use `./gradlew` / `gradlew.bat`

## Build

```bash
./gradlew clean test
```